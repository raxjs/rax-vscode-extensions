import { createElement } from 'rax';
import View from 'rax-view';
import Text from 'rax-text';

import './index.css';

interface <%= componentName %>PageProps {
  name?: string
}

export default function <%= componentName %>(props: <%= componentName %>PageProps) {
  const { name } = props;
  return (
    <View className="<%= componentClassName %>">
      {name ? <Text className="title">Hello {props.name}</Text> : null}
      <Text className="title">Welcome to Your Rax App</Text>
      <Text className="info">More information about Rax</Text>
      <Text className="info">Visit https://rax.js.org</Text>
    </View>
  );
}
